num_of_student=int(input("Enter the number of students:-"))
for i in range(1,num_of_student+1):
    total=0
    largest=0
    lowest=100
    student_name=str(input("Enter the student name:-"))
    Roll_number=int(input("Enter the roll number:-"))
    num_of_subject=int(input("Enter the number of subject:-"))
    student_marks={}
    for j in range(num_of_subject):
        subject_name=input("Enter the subject name:-")
        marks=int(input("Enter the marks:-"))
        if marks<0 or marks>100:
            print("marks should be between 0 and 100")
            quit()
        student_marks[subject_name] = marks
    for subject_name in student_marks:
        total=total+student_marks[subject_name]
        if student_marks[subject_name]>largest:
            largest=student_marks[subject_name]
        if student_marks[subject_name]<lowest:
            lowest=student_marks[subject_name]
    avg_marks=total/num_of_subject
    if avg_marks>=90:
            grade="A"
    elif avg_marks>=80:
            grade="B"
    elif avg_marks>=70:
            grade="C"
    elif avg_marks>=60:
            grade="D"       
    else:
            grade="F"
    print("student name:-",student_name)
    print("roll number:-",Roll_number) 
    print("total marks:-",total)
    print("marks in each subject:-",student_marks)
    print("average marks:-",avg_marks)
    print("grade:-",grade)
    print("largest marks:-",largest)
    print("lowest marks:-",lowest)